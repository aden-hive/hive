# Git Client Tool

Allows agents to interact with Git repositories safely.

## Capabilities
- `git_status`: Check for modified files.
- `git_log`: Read commit history.

## Safety
- Non-interactive mode enforced (prevents hanging).
- Read-only operations default.