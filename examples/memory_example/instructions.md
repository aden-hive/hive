# Instructions: Running the Memory Example (STM + LTM)

This guide shows how to validate and simulate the memory plan using the Aden Hive MCP Agent Builder.

> You do **not** need to modify any framework code to use this example.

---

## 1) Setup (MCP Agent Builder)

From the repo root:

```bash
cd core
python setup_mcp.py
python verify_mcp.py
